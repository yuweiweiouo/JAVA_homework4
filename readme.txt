使用cmd 
輸入
 > ant jar
輸入後

有兩種方式開啟程式

1.
先點 RunServer.bat 再點 RunClient.bat 
2.
進入dist資料夾，直接點擊Server.jar再點Client.bat

使用方法
Server: 執行程式後，會自動等待Clinet進行連線，連線後可以輸入訊息傳送給所有clients

Client: 執行程式後，要先取個名字，並且在左下角輸入想連線的port號，可以同時加入多個Servers，並同時傳送給多個servers
